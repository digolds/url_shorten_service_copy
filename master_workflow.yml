version: 0.2
env:
  variables:
    S3_BUCKET: "movies-api-deployment-packages"
    PACKAGE: "github.com/mlabouardy/lambda-codepipeline"

phases:
  install:
    commands:
      - echo "install step"

  pre_build:
    commands:
      - python -B -m unittest -v tests.test_generate_a_shorter_url.TestGenerateId

  build:
    commands:
      - echo "build step"

  post_build:
    commands:
      - echo "post_build step"
